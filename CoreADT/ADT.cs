namespace CoreADT
{
    class ADT
    {
        // MODF = chunkBytes.Length / 64 Chunks
        // MDDF = chunkBytes.Length / 36 Chunks
        // MH2O = 256 Chunks
        // MCIN = 256 Chunks
    }
}
